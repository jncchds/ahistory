<Project Sdk="Microsoft.NET.Sdk">

  <!-- Settings live in Directory.Build.props. Do not repeat them here. -->
  <!-- The desktop head stays thin: AppBuilder, DI wiring, publish targets. UI lives in Archive.Ui. -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../Archive.Ui/Archive.Ui.csproj" />
    <ProjectReference Include="..\Archive.Logging\Archive.Logging.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia.Desktop" Version="12.1.2" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="10.0.12" />
  </ItemGroup>

</Project>
