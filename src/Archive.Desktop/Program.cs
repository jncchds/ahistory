// Archive.Desktop — the desktop head.
//
// Stays thin by design: AppBuilder, DI wiring and publish targets only. Every view and
// view model lives in Archive.Ui so the headless UI tests never need an AppBuilder.
// Avalonia bootstrap arrives in M4.

Console.Error.WriteLine("ahistory — desktop shell not implemented yet (M0 skeleton).");
return 1;
